# secrets / AWS resource files -- never commit
illation-key.pem
instance_id.txt
instance_ip.txt
sg_id.txt

# environment
venv/
__pycache__/
*.pyc

# large / regenerable data
data/corpus.txt
data/dracula_raw.txt
data/books/
tokenizer_files/
runs/**/*.pt
runs/**/*.safetensors
runs/**/best/
runs/**/final/

# logs
run_both.log
*.log

# per-user online-adaptation weight state: its own separate git repo (history/rollback
# for user weights), deliberately NOT tracked inside the code repo -- see user_state.py
user_states/
